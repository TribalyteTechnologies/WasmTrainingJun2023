#ifndef ELECTRONDESKTOPAPPEXAMPLE_WEB_ELECTRONDESKTOPAPPEXAMPLE_H_
#define ELECTRONDESKTOPAPPEXAMPLE_WEB_ELECTRONDESKTOPAPPEXAMPLE_H_
#include <iostream>

class Calculator {
 public:
  Calculator();
  ~Calculator();
  double addNumbers(float a, float b);
  double substractNumbers(float a, float b);
  double multiplyNumbers(float a, float b);
  double divideNumbers(float a, float b);
};
#endif  // ELECTRONDESKTOPAPPEXAMPLE_WEB_ELECTRONDESKTOPAPPEXAMPLE_H_
